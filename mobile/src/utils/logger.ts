export const logError = (error: any) => {
    console.error(error);
  };
  
  export const logInfo = (info: any) => {
    console.info(info);
  };
  
  export default {
    logError,
    logInfo,
  };
  